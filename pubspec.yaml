name: pomodoro_timer
description: "A cute Pomodoro timer with campus gallery gamification to keep you focused and motivated."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.7.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  audioplayers: ^6.4.0
  shared_preferences: ^2.3.4
  cached_network_image: ^3.4.1
  flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3 # You likely have this already
  rename: ^3.0.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
  - assets/alarm.mp3
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

fonts:
    - family: TimerFont
      fonts:
        - asset: assets/fonts/Noto.ttf
          weight: 400
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  image_path: "assets/launcher_icon/icon_mac.png"
  # Android settings
  android: true # Generate Android icons
  adaptive_icon_foreground: "assets/launcher_icon/icon_android_fg.png"
  adaptive_icon_background: "#fec18a"

  # iOS settings
  ios: true # Generate iOS icons
  image_path_ios: "assets/launcher_icon/icon_mac.png" # Can be same or different
  remove_alpha_ios: true # Recommended for iOS icons

  # macOS settings (Optional, add if needed)
  macos:
    generate: true # Generate macOS icons
    image_path: "assets/launcher_icon/icon_mac.png" # Path to YOUR source icon

  windows: 
    generate: true # Generate Windows icons
    image_path_windows: "assets/launcher_icon/icon_mac.png" # Path to YOUR source icon

  web: 
    generate: true # Generate Web icons
    image_path_web: "assets/launcher_icon/icon_android_fg.png" # Path to YOUR source icon

  # Other platforms like web, windows, linux can also be configured

flutter_native_splash:
  # Background color for LIGHT theme (Android < 12 fallback & iOS default)
  color: "#FFFBF5" # Your light theme background (Cream)

  # Background color for DARK theme (Android < 12 fallback & iOS default)
  color_dark: "#242038" # Your dark theme background (Indigo)

  # Icon for Android < 12 fallback and iOS.
  # Use the transparent version here. The package should render it
  # on top of the 'color'/'color_dark' defined above for these fallbacks.
  image: assets/launcher_icon/icon_mac.png # <<< PATH TO YOUR TRANSPARENT ICON

  # --- Android 12+ Specific Settings ---
  android_12:
    # The transparent icon image. The system places this on the window background.
    image: assets/launcher_icon/icon_mac.png # <<< PATH TO YOUR TRANSPARENT ICON

    # Window background color for Android 12+ Splash Screen API
    color: "#FFFBF5" # Light window background (matches theme)
    color_dark: "#242038" # Dark window background (matches theme)

  # --- IMPORTANT ---
  # DO NOT define 'icon_background_color' or 'icon_background_color_dark' here
  # if you want the icon's transparency to show the main window background.
  # Leave them commented out or delete them entirely from the android_12 section.
  # icon_background_color: "#SomeColor" # DELETE or COMMENT OUT
  # icon_background_color_dark: "#SomeDarkColor" # DELETE or COMMENT OUT

  # Other platforms can be configured if needed, but focus is Android
  android: true
  ios: false
  web: false